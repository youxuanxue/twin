# Twin

Twin is a provider-neutral supervisor for evidence-driven agent work.

Python 3.9 or newer is required.

Install for development with:

```bash
uv tool install --editable .
```

This repository is not part of dev-rules.
